services:
  pgadmin:
    image: dpage/pgadmin4
    container_name: pgadmin_container
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: bianquiviri@gnail.com
      PGADMIN_DEFAULT_PASSWORD: !N1k00905
    ports:
      - "5050:80"

volumes:
  pgadmin_data:
